# Lab 1: Classes, Objects, Encapsulation
Java Programming Lab
Paramet Keawpool 6630613033 DE

From the `OOP` folder, compile the files:

```powershell
cd lab1
javac *.java
```
So we are able to run those files in lab1 folder

To run question I want to see result:

```powershell
java BookDemo              # Q1
java -ea EmployeeTest      # Q2
java -ea EmployeeTeamTest  # Q3
java -ea AccountTest       # Q4
java -ea AccountListTest   # Q5
```

`-ea` enables the test checks. Results appear in the terminal.
Saved results and explanations are in [output.txt](output.txt).
